# Як долучитися до розробки (Contributing)

Ми дуже раді, що ви хочете допомогти розвитку **CL Launcher**! 🎉
Наш проект створюється ентузіастами, і ми цінуємо будь-який внесок — від виправлення дрібних помилок до нових функцій.

## 🎨 Стиль коду (Code Style)

У нас немає суворого багатосторінкового гайду по стилю, але ми просимо дотримуватися кількох простих правил для збереження читабельності:

1.  **Дотримуйтесь існуючого стилю:** Якщо ви редагуєте файл, намагайтеся писати так, щоб ваш код візуально не відрізнявся від коду, який вже там є.
2.  **Читабельність понад усе:** Пишіть код так, щоб інші могли його зрозуміти. Використовуйте зрозумілі назви змінних (англійською мовою).
3.  **Автоформатування:** Перед тим як робити коміт, використовуйте функцію форматування вашого середовища розробки:
    * У **Visual Studio**: `Ctrl + K`, потім `Ctrl + D`.
    * У **Rider** або **VS Code**: `Alt + Shift + F` (або відповідна комбінація).
4.  **Стандарти C#:** За замовчуванням ми використовуємо стандартні конвенції C# (PascalCase для методів та класів, camelCase для локальних змінних), якщо в конкретному файлі не використовується інше.

## 🚀 Як внести зміни (Workflow)

1.  **Зробіть Fork** цього репозиторію (кнопка зверху праворуч).
2.  Склонуйте свій форк на комп'ютер.
3.  Створіть нову гілку для вашої задачі:
    ```bash
    git checkout -b feature/NewCoolFeature
    # або
    git checkout -b fix/BugFixName
    ```
4.  Внесіть зміни та перевірте, чи проект запускається.
5.  Закомітьте зміни (Commit) зі зрозумілим описом:
    * ✅ Добре: `Add support for Modrinth search`
    * ❌ Погано: `fix code`, `update`
6.  Зробіть **Push** у свій форк.
7.  Створіть **Pull Request (PR)** у головний репозиторій.

## ⚠️ Важливі зауваження

* **API Ключі:** Ніколи не заливайте свої приватні API ключі (CurseForge, Microsoft, etc.) у код.
* **Ліцензія:** Вносячи зміни, ви погоджуєтесь, що ваш код буде розповсюджуватися під ліцензією **GPLv3**, яку використовує цей проект.

Дякуємо за допомогу! Разом ми зробимо найкращий український лаунчер! 🇺🇦
